# Newsline demo
1. Tools → Import → upload `demo.wxr`.
2. Menu: Latest `/blog/`, About `/about/`, Newsletter `/newsletter/`, Contact `/contact/`.
3. Settings → Reading → Homepage: Home (uses newsline/front-page), Posts: Blog.
