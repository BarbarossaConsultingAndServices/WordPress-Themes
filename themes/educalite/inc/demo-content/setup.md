# Educalite demo content
1. Tools → Import → upload `demo.wxr`.
2. Menus: Home `/`, Courses `/courses/`, Instructors `/instructors/`, Events `/events/`, Blog `/blog/`, Contact `/contact/`.
3. Settings → Reading → Homepage: Home, Posts: Blog.
